### Day 61 – Count Files by Type (User Input)
- Learned to use `read` to take user input in Bash.
- Combined `find` and `wc` to count files dynamically.
- Practiced basic user interaction in scripts.
