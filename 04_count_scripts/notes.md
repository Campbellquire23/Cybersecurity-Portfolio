### Day 60 – Count .sh and .py Files
- Practiced combining multiple `find` commands in one script.
- Used Bash arithmetic `$((...))` to total results.
- Builds on previous file counter scripts to handle multiple types.
