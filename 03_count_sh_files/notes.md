### Day 59 – Count Only `.sh` Files
- Practiced using `find` with `-name` to filter specific extensions.
- Combined it with `wc -l` to count matching files.
- Useful for automation, backups, or cleanup scripts.
